# Tasks

## 1. 依存の更新

- [x] 1.1 `gradle/libs.versions.toml` の `fankt` を `0.1.1` から `0.1.3` へ更新する

## 2. guest 経路の宣言

- [x] 2.1 `FanboxRepository.kt` に配信先の manifest URL、鍵名、Ed25519 公開鍵を `private` 定数として置く
- [x] 2.2 `FanboxRepositoryImpl` の `Fanbox` 生成を、配信先と公開鍵を受け取るコンストラクタへ差し替える
- [x] 2.3 埋め込んだ公開鍵が 32 バイトへ復号されることを確かめる最小のテストを置く

## 3. 鍵の妥当性の確認

- [x] 3.1 埋め込んだ公開鍵が、配信中の manifest の署名を検証できることを確認する（誤った鍵は無言の機能停止として現れるため）

## 4. ドキュメント

- [x] 4.1 遠隔から解析処理を取得することと、用途を FANBOX の仕様変更への追従に限ることを `README.md` に記載する

## 5. 検証

- [x] 5.1 `detekt` と単体テストを実行する
- [x] 5.2 Android のリリースビルドが通ることを確認する
- [x] 5.3 iOS 向けのコンパイルが通ることを確認する（変更は commonMain にあり、両プラットフォームへ届く）
